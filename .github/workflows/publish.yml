name: Publish to npm (OIDC)

# Triggered on tag vX.Y.Z. The workflow publishes @kuramalab-io/mysupportdetails-mcp
# using the GitHub Actions OIDC identity as trusted publisher.
# See https://docs.npmjs.com/trusted-publishers
on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      # id-token: write is required for OIDC provenance
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      # npm >= 11.5.1 required for full trusted publisher OIDC support
      - name: Upgrade npm
        run: npm install -g npm@latest

      - name: Install deps
        run: npm ci

      - name: Typecheck + test + build
        run: |
          npm run typecheck
          npm run test
          npm run build

      - name: Publish (OIDC, provenance)
        run: npm publish --provenance --access public
